# `components` — 작은 UI 부품

페이지 전체가 아니라 특정 기능을 담당하는 Vue 컴포넌트입니다.

- `game/`: 실제 게임 중 사용하는 UI
- `lobby/`: 방 생성/입장 관련 UI
- `layout/`: 여러 화면에서 공통으로 보이는 헤더, 오류, 사운드 컨트롤

컴포넌트는 가능하면 서버 통신을 직접 하지 않고, `props`로 데이터를 받고 `emit`으로 사용자의 행동을 부모에게 알려줍니다. 이렇게 하면 UI와 통신 로직의 결합도가 낮아집니다.
